import { Container } from '@/shared/ui/container';

export const NavigationBar = () => {
    return (
        <nav className="bg-red-500">
            <Container variant="navigation">Navigation!!</Container>
        </nav>
    );
};
